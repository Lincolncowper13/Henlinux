name: HenLinux

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  expose:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Install Docker
        run: |
          sudo apt-get update
          sudo apt-get install -y apt-transport-https ca-certificates curl software-properties-common
          curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/trusted.gpg.d/docker-archive-keyring.gpg
          echo "deb [arch=amd64] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list
          sudo apt-get update
          sudo apt-get install -y docker-ce docker-ce-cli containerd.io

      - name: Build HenLinux image
        run: |
          sudo docker build -t henlinux .

      - name: Run HenLinux container
        run: |
          sudo docker run -d --name henlinux-vps -p 2222:22 henlinux

      - name: Wait for container initialization
        run: |
          sleep 10

      - name: Check if HenLinux container is running
        run: |
          container_status=$(sudo docker inspect -f '{{.State.Running}}' henlinux-vps || echo "false")
          if [ "$container_status" != "true" ]; then
            echo "HenLinux container is not running. Checking logs..."
            sudo docker logs henlinux-vps
            exit 1
          fi

      - name: Debug Container Logs
        run: |
          sudo docker logs henlinux-vps

      - name: Save Ngrok authtoken and Start Tunnel
        run: |
          sudo docker exec henlinux-vps bash -c "/usr/local/bin/ngrok authtoken 2pR0fPCjO8MUjY9RACaVS3xlrZE_6YkBEbjHxAmvvjhmRqFur && /usr/local/bin/ngrok tcp 2222"

      - name: Get Ngrok public SSH URL
        run: |
          sleep 10
          ngrok_url=$(curl --silent http://localhost:4040/api/tunnels | jq -r .tunnels[0].public_url)
          if [ -z "$ngrok_url" ]; then
            echo "Ngrok URL is empty. Checking ngrok logs."
            sudo docker logs henlinux-vps
            exit 1
          else
            echo "Ngrok public SSH URL is: $ngrok_url"
            echo "ngrok_url=$ngrok_url" >> $GITHUB_ENV
